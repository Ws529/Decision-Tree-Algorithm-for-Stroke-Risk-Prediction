# setuptools and wheel help build binary packages; avoid listing pip itself
setuptools>=65.5.0
wheel

streamlit==1.31.0
# Pin numpy/pandas to widely supported wheel versions to avoid source builds on the server
numpy==1.25.3
pandas==2.1.2
plotly==5.18.0
scikit-learn==1.4.0
joblib==1.3.2
matplotlib==3.8.2
seaborn==0.13.1
